var sio = require('socket.io');
module.exports = {
	Game: require('./Game.js'),
	listen: function(connect_server) {
		var io = sio.listen(connect_server);
		var game = new module.exports.Game(io);
		game.run();
	}
}